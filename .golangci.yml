version: "2"
run:
  timeout: 3m
  go: "1.27"
linters:
  default: standard
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - misspell
    - revive
    - gosec
    - modernize
  exclusions:
    generated: lax
    paths:
      - web/node_modules
    rules:
      - path: pkg/client/generated/generate.go
        linters:
          - revive
        text: "package-comments:"
formatters:
  enable:
    - gofmt
    - goimports
  exclusions:
    generated: lax
    paths:
      - web/node_modules
